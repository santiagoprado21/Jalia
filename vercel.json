{
  "buildCommand": "pnpm --filter @workspace/calculadora-postres build && OUT=artifacts/calculadora-postres/dist/public && [ -d \"$OUT\" ] || OUT=dist/public && rm -rf public && cp -r \"$OUT\" public",
  "outputDirectory": "public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}

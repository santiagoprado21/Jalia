{
  "installCommand": "cd ../.. && pnpm install --no-frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @workspace/calculadora-postres build && cd artifacts/calculadora-postres && rm -rf public && cp -r dist/public public",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
